[package]
name = "beekem-vectors"
version = "0.1.0"
edition = "2021"

[dependencies]
beekem = { path = "../keyhive/beekem"  # clone inkandswitch/keyhive next to beekem-vectors (see ../README.md) }
keyhive_crypto = { path = "../keyhive/keyhive_crypto" }
ed25519-dalek = "2.1"
rand = "0.8.5"
serde_json = "1"
hex = "0.4"

[workspace]

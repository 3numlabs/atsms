[package]
name = "beekem-vectors"
version = "0.1.0"
edition = "2021"

[dependencies]
# Path dependencies on the upstream crates. Clone inkandswitch/keyhive next to
# this directory (oracle/keyhive) before building — see ../README.md.
beekem = { path = "../keyhive/beekem" }
keyhive_crypto = { path = "../keyhive/keyhive_crypto" }
ed25519-dalek = "2.1"
rand = "0.8.5"
serde_json = "1"
hex = "0.4"

[workspace]

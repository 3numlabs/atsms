{
  "name": "atsms",
  "private": true,
  "description": "ATSMS — an open protocol for end-to-end encrypted messaging on AT Protocol identities: specifications, record schemas, and the TypeScript reference implementation",
  "license": "Apache-2.0",
  "author": "3NUM",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun run --cwd packages/dcgka test && bun run --cwd packages/client test",
    "test:unit": "bun run --cwd packages/dcgka test:unit && bun run --cwd packages/client test",
    "test:fuzz": "bun run --cwd packages/dcgka test:fuzz",
    "typecheck": "bun run --cwd packages/dcgka typecheck && bun run --cwd packages/client typecheck",
    "build": "bun run --cwd packages/dcgka build-types && bun run --cwd packages/client build"
  }
}
